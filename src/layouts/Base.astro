---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  transparentHeader?: boolean;
  currentPath?: string;
}

const {
  title,
  description = 'Personal and unique designs for living spaces. Interior design for homes, offices, restaurants and bars in London.',
  transparentHeader = false,
  currentPath = Astro.url.pathname,
} = Astro.props;

const fullTitle = title === 'Home' ? 'Alexandre Interiors' : `${title} — Alexandre Interiors`;

const navItems = [
  { href: '/about', label: 'About' },
  { href: '/services', label: 'Services' },
  { href: '/portfolio', label: 'Portfolio' },
  { href: '/inspiration', label: 'Inspiration' },
  { href: '/blog', label: 'Blog' },
  { href: '/press', label: 'Press' },
  { href: '/contact-us', label: 'Contact' },
];

const isActive = (href: string) => {
  if (href === '/') return currentPath === '/' || currentPath === '/index.html';
  return currentPath === href || currentPath.startsWith(`${href}/`);
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="icon" href="/images/1502979797795-KXFWRA4NXHJH3X5LST7Q/favicon.ico" />
    <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site).href} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet" />
    <title>{fullTitle}</title>
  </head>
  <body class:list={[{ 'transparent-header': transparentHeader, 'has-banner-image': transparentHeader }]}>
    <header id="header" class="site-header">
      <div class="header-inner">
        <div id="logoWrapper">
          <a href="/">
            <img
              src="/images/1533459442163-4PXFYJ3ZVVFMWKL3RP9J/Alexandre-Interiors-White.png"
              alt="Alexandre Interiors"
              width="220"
              height="46"
            />
          </a>
        </div>

        <button class="mobile-nav-toggle" aria-label="Toggle menu" aria-expanded="false">
          <span></span><span></span><span></span>
        </button>

        <nav id="mainNavigation" class="site-nav" aria-label="Main">
          {navItems.map((item) => (
            <a href={item.href} class:list={[{ active: isActive(item.href) }]}>
              {item.label}
            </a>
          ))}
        </nav>
      </div>
    </header>

    <main id="page" role="main">
      <slot />
    </main>

    <script>
      const header = document.getElementById('header');
      const toggle = document.querySelector('.mobile-nav-toggle');
      const nav = document.querySelector('.site-nav');

      toggle?.addEventListener('click', () => {
        const open = nav?.classList.toggle('open');
        toggle.setAttribute('aria-expanded', String(!!open));
        document.body.classList.toggle('nav-open', !!open);
      });

      if (document.body.classList.contains('transparent-header')) {
        const onScroll = () => {
          header?.classList.toggle('is-scrolled', window.scrollY > 80);
        };
        onScroll();
        window.addEventListener('scroll', onScroll, { passive: true });
      }
    </script>
  </body>
</html>
